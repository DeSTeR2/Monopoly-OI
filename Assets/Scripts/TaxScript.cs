using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TaxScript : MonoBehaviour
{
    public int taxAmound;
    // Start is called before the first frame update

    // Update is called once per frame
}
